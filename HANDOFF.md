# 인계문서

2026-09-18 기준 (3차 갱신 — 읽히기·신화 고증·알림층까지). 이 문서는 "지금 어디까지 됐고, 다음 사람이 무엇부터
집으면 되는가" 만 적는다. 왜 그렇게 만들었는지는 각 파일 맨 위 주석에
있다 — 이 저장소는 주석에 판단 근거를 남기는 쪽으로 쓰고 있다.

---

## 1. 한 문단 요약

three.js 쿼터뷰 로그라이크. 오디세우스의 귀향 여덟 판, 보스 일곱.
전투는 **프레임 데이터**로 돌아간다 (`startup → active → recovery`,
`cancelAt`). 물리 엔진은 없고 XZ 평면의 원·부채꼴 거리 계산만 쓴다.
판 모양은 `src/stage/arena.js` 한 군데가 정한다 — 원, 네모, 갑판, 동굴,
숲, 만 일곱 가지.

```bash
npm install
node node_modules/esbuild/install.js   # allow-scripts 가 postinstall 을 막으면 필요
npm run dev                            # http://localhost:5180
```

개발용 열쇠 —

| 키 | 하는 일 |
|---|---|
| `Tab` | 연출·판 고르기 패널 (컷신 하나만 틀어 보기) |
| `]` | 다음 판 |
| `R` | 처음부터 |
| `L` | 화면 톤 돌려보기 |
| `M` | 음소거 |
| `G` | 시험용 무적 끄기 (기본 켜짐 — **배포 전에 반드시 꺼야 한다**) |

URL 인자: `?god=0` 무적 끔, `?stage=N` 그 판부터, `?boss=1` 보스방 바로,
`?sail=story` 난이도 고르기 건너뛰기, `?bare=1` 성장 없이.

콘솔 도구 (`import.meta.env.DEV` 에서만):
`__game`, `__tick(n)` 프레임 n 개 강제 진행, `__seek(sec)` 그림만 진행,
`__shot('a.png')` `shots/` 로 화면 저장.

> **숨은 창에서는 rAF 가 멈춘다.** 브라우저 패널이 가려져 있으면
> 게임이 4~30fps 로 보이는데 그건 게임이 느린 게 아니라 rAF 조절이다.
> 렌더 한 번은 1.43~1.73ms 로 측정된다 (577~700fps 여유).
> 가려진 창에서 확인할 때는 `__tick` / `__seek` 로 손으로 밀어야 한다.

---

## 0.9 12차 갱신 (2026-09-22) — 칼질 겹침 고치고, 동굴·텔레필로스도 스톱모션

### 칼 휘두를 때 자세 두 장이 겹쳐 보이던 것
`character.js` 의 애니메이션은 전신 클립과 "코어"(상반신만) 클립을
동시에 트는 이중 레이어 구조다. 두 레이어의 `fade` 타이밍이 어긋나
있어서 칼 스윙 시작 순간 이전 자세와 새 자세가 겹쳐 보였다 — 코어
레이어의 페이드 시점을 전신 레이어와 맞춰서 고쳤다.

### 동굴·텔레필로스에도 CUT_BOW 식 스톱모션을
`cuts.js` 주석에 이미 적혀 있던 대로, `CUT_CAVE` 의 바위가 문을
막는 장면과 `CUT_TELEPYLOS` 의 돌이 떨어지는 장면은 둘 다 그림
한 장을 포커스만 바꿔 우려먹고 있었다 — 정작 "동작" 자체는 한 번도
안 보여 준 자리다. 각각 새 그림을 받아 `hard: true` 3장/2장 하드컷
시퀀스로 바꿨다:

- 동굴: `cave-boulder-1.webp`(아직 안 막힘) → 기존 `cut-cave-2.webp`
  (반쯤) → `cave-boulder-3.webp`(거의 다 막힘).
- 텔레필로스: `telepylos-rock-1.webp`(돌이 공중에) → 기존
  `lude-telepylos-b.webp`(부서진 뒤, 전경).

`reel.js` 의 `s.text !== undefined` 규칙대로 중간 프레임은 자막을
비워서 앞 자막이 그대로 붙어 있게 했다. `ALL_CUTS`/`cutArt()` 가
`art:` 필드를 자동 수집하므로 프리로드 등록은 따로 안 건드렸다.

---

## 1.0 11차 갱신 (2026-09-22) — 뱃길에서 섬이 안 보이고 있었다

### 실루엣이 안 보이면 실루엣이 아니라 그냥 안 보이는 것이다
`props.js` 의 `buildIsle` 은 애초에 "멀리서 실루엣으로만 보인다" 는
전제로 만들었다. 그런데 실제로 항해 화면을 캡처해서 확인해 보니
**목적지 섬이 통째로 안 보이고 있었다** — 바위 색(`#5c646a`)이 안개·
하늘색(`#6d7f8c`·`#7d8f9c`)과 명도가 거의 같아서, 실루엣이 아니라
그냥 안개에 먹힌 것이었다. 코드에 적힌 의도(실루엣으로 보인다)와
실제 화면(안 보인다)이 갈려 있었는데, 콘솔로 카메라·섬 좌표를 재고
`__shot` 으로 실제 프레임을 캡처해서야 잡혔다.

- 색을 하늘·안개보다 뚜렷이 어둡게 냈다 (`#5c646a`→`#252c33` 등).
- 밑동에 발광(emissive) 흰 파도띠를 둘렀다 — 색이 아무리 어두워도
  "물과 맞닿는 선" 하나는 항상 밝게 남아서, 봉우리가 화면 위로 잘려
  나가는 거리에서도 뭍이 있다는 것 자체는 읽힌다.

### 조용한 게 "잘 가고 있다" 가 아니라 "멈췄다" 로 읽혔다
`SailLeg` 은 "W/S 돛, A/D 키" 를 3.2초만 띄우고, 그 뒤로는 **빗나갔을
때만** 방향을 말해 줬다. 맞게 가고 있으면 화면이 조용한데, 그 조용함이
"잘 가고 있다" 가 아니라 "뭘 해야 할지 모르겠다" 로 읽혔다 (섬이 화면
안에 없는 구간이면 더더욱). 늘 떠 있는 작은 나침반(`sailleg.js` 의
`COMPASS_CSS`)을 더했다 — 뱃머리 기준 섬의 상대각으로 바늘을 돌리고,
그 밑에 남은 거리(m)를 계속 보여 준다. 화살표가 위를 가리키면 똑바로
가고 있는 것이다.

### 액자→바다 전환은 이미 되고 있었다
`meltToSea`(`ui/interlude.js`)와 `Game.sailTo`(`main.js`) 를 다시
읽고 실제로 여러 번 재생해 봤는데, 막간 액자가 물처럼 흐려지며 그
자리에 바다가 깔리는 전환 자체는 판마다 (저승행처럼 액자를 안 쓰는
자리만 빼고) 빠짐없이 이미 걸려 있었다. 여기는 고칠 게 없었다 — 위
두 가지(섬이 안 보이던 것, 가이드가 끊기던 것)가 "뱃길이 허전하다"
는 인상의 실제 원인이었다.

---

## 1.1 10차 갱신 (2026-09-22) — 키르케·세이렌에게도 등장 장면을

### 두 보스만 컷신이 없었다
여덟 판 중 여섯 곳(이스마로스·텔레필로스·저승·메시나·이타카·활 겨루기)은
보스를 만나기 전에 컷신이 떴다. 그런데 **키르케(아이아이에섬)와
세이렌(세이렌의 바다)만 없었다** — `stages.js` 의 두 `boss` 항목에 `cut:`
필드 자체가 없었다. 그동안 `boss/kirke.webp`·`boss/siren.webp` 세로
초상은 이미 있었는데 아무 데서도 안 쓰이고 있었다 — 만들어 놓고 쓸
자리를 안 붙인 채였다.

『오디세이아』에서 이 둘은 가장 유명한 장면에 속한다 (사람을 돼지로
바꾸는 마녀, 돛대에 몸을 묶고 듣는 노래). 그게 현판 한 줄로만 지나가는
건 이 판이 대접받을 값에 안 맞는다 싶어 그림을 새로 만들었다.

- **`CUT_AIAIA`** (`src/stage/cuts.js`) — 안개 낀 숲 속 연기 나는 저택
  (넓게 → 당겨서) → **돼지 우리에 갇힌, 눈만은 사람인 짐승들** →
  `boss/kirke.webp`. 셋째 컷이 이유다 — 이게 있어야 마지막 초상의
  웃음이 무섭다.
- **`CUT_SIRENS`** — 돛대에 묶인 오디세우스, 밀랍으로 귀를 막는 동료들
  (넓게 → 동료 쪽으로 당겨서) → `boss/siren.webp`. 세이렌은 홀리는
  것이라 메시나처럼 긴 준비가 필요 없어서 세 컷으로 짧게 끊었다.

새 그림 세 장(`cut-aiaia-1/2.webp`, `cut-sirens-1.webp`)은 지금까지와
같은 절차로 만들었다 — 제미나이에서 16:9로 생성하고, 이미지를 클릭해
편집기로 들어가서 ⬇ 버튼으로 내려받고, `cwebp` 로 줄였다. `stages.js`
의 `kirke`·`siren` 보스 항목에 `cut:` 을 달고, `Tab` 패널로 두 컷신 다
틀어서 확인했다.

### 동작 하나를 스톱모션으로 — `reel.js` 에 `shot.hard`
지금까지 릴(`ui/reel.js`)은 장이 바뀔 때마다 크로스페이드로 넘겼다.
장면이 바뀌는 데는 맞는데, **화살이 날아가는 것처럼 한 동작이 이어지는
순간**을 그렇게 넘기면 두 그림이 흐물흐물 녹아 붙어서 오히려 안
날아가는 것처럼 보인다. 그래서 `shot.hard: true` 를 추가했다 — 짧게
(700~950ms) 여러 장을 거의 겹치지 않고 컷으로 바로 넘기면, 초당
한두 장짜리 낮은 프레임률 애니메이션처럼 읽힌다.

같이 고친 버그 하나: 릴은 shot 이 바뀔 때마다 글자를 무조건
페이드아웃했다가 `s.text` 가 있으면 다시 채웠다 — 스톱모션처럼 한
문장 위로 그림 여러 장이 지나가는 구간에서 `text` 를 안 적어 둔 shot
마다 글이 깜빡였을 것이다. 이제 `s.text !== undefined` 일 때만 글을
건드린다.

**시연**: `CUT_BOW` 의 "화살 하나가 열두 자루를 꿰었다" 순간을 네 컷
(`cut-bow-arrow-1/2/3` + 기존 `cut-bow-3`) 으로 늘렸다. 화살이 첫
고리에 닿기 직전 → 조금 더 들어간 자리 → 더 들어간 자리 → 완전히
관통(기존 그림) 순으로, 750→700→700→950ms 마다 하드컷한다. 실제
DOM 을 폴링해서(`.plate.in` 이 몇 번인지, 어느 그림인지) 넷이 정확한
순서·타이밍으로 넘어가는 것까지 확인했다.

---

## 1.2 9차 갱신 (2026-09-22) — 칼질·활쏘기 고치고, 컷신 그림 세 장

### 팔만 도는 동작에서 몸통이 굳던 것 (칼·활)
빌려 온 팔 동작(`Sword_A/B/C`, `Bow_Draw`, `Bow_Shoot`)은 팔 8개 뼈만
움직이는 클립이다. 그런데 `character.js` 는 액션을 하나만 틀고
크로스페이드로 갈아 끼웠다 — 팔 클립으로 넘어가는 순간, 몸통·다리·골반은
**직전 클립(대개 달리기 중간 자세)에서 그대로 얼어붙었다.** 달리다가
공격이 나가면 허리가 꺾인 것처럼 보인 이유가 이거였다.

두 켜로 나눴다. 팔 클립을 트는 액션과 별개로, 팔 뼈를 뺀 "몸통만" 클립
(`stripArmTracks`, idle/run/sprint 세 가지를 미리 만들어 둠)을 두 번째
레이어로 같이 튼다 — 팔은 빌려 온 동작이 잡고, 몸통은 지금 달리는 속도에
맞는 자세를 계속 잡는다. 프레임을 직접 찍어 (`g.loop.stop()` 으로 실제
루프를 멈추고 수동으로 프레임을 밀어) 전후를 비교해 확인했다.
→ `src/render/character.js`

### 컷신 그림 세 장 — 3절에 적혀 있던 대역을 다 갈았다
제미나이(무료 계정, 16:9)로 직접 만들었다:
- 동굴 안에서 입구를 본 구도 (양 우리·치즈), 폴리페모스가 바위로 문을
  막는 순간 — 두 장 다 `boss/antiphates`(다른 거인) 대역을 쓰던 자리.
- 저승 내부 — 해가 들지 않는 잿빛 벌판. `lude-ithaca`(무너진 홀) 대역
  자리.
- 절벽에 붙은 스킬라 — 돌로 된 크라켄이 절벽에서 뻗어 배를 노리는 구도.
  `lude-aiaia-b`(안개 속 절벽, 스킬라 없음) 대역 자리.

`art/gen/` 에 원본을 두고 `cwebp` 로 `public/img/cut-*.webp` 로 줄였다.
`src/stage/cuts.js` 의 `art:` 와 `focus` 를 갈아 끼웠고, `Tab` 패널로
다섯 컷 다 실제로 틀어서 글과 그림이 맞는지 확인했다. 더 이상 다른
장면의 그림을 빌려 쓰는 자리가 없다.

### 운명의 갈림길·죽음 화면을 실제로 다 틀어서 훑었다
`src/ui/fate.js` 네 장(이름·헤르메스·한 해·헬리오스의 소)을 콘솔에서
`game.chooseFate()` 로 하나씩 직접 띄워 그림·글·선택·결과·신 눈금까지
확인했다 — 그림 네 장 다 있고 결과도 맞게 반영된다. 부족했던 건 죽음
화면(`hud.js` 의 `credits()`) 쪽이었다: 무엇을 골랐는지 목록에는 나오는데,
**그 선택이 죽음과 어떻게 이어지는지는 말이 없었다.** `사망 문구` 는
늘 고정("창은 가오리 뼈로...")이라 네 번을 다르게 골라도 마지막 화면은
매번 같았다.

그래서 갈림길의 무거운 선택 두 개(이름을 밝힐지, 헬리오스의 소에
손댈지)에 `epilogue` 한 줄을 붙였다. 가장 나중에 고른 갈림길의 한
줄을 죽음 화면 소제목 위에 띄운다 — 이름을 밝히면 "포세이돈은 끝내
잊지 않았다", 소에 손대지 않고 다 잃으면 "그래도 바다는 그를 놓아주지
않았다" 식으로, 그 선택이 결국 이 죽음까지 왔다는 말을 남긴다.
`voyage.js` 의 `FATES` 두 곳, `hud.js` 의 `credits()`.

---

## 1.3 8차 갱신 (2026-09-22) — 훑어서 고친 것들

지시 없이 "알아서" 훑은 회차. 새 기능은 없고 전부 고침이다.

### 진짜 버그 (동작이 틀렸던 것)
- **놀란 양이 벽을 뚫고 나갔다.** 평소 자리는 `arena.radiusAt` 으로 안쪽만
  고르는데 도망치는 방향은 그 검사를 건너뛰었다 (`enemy/sheep.js`).
- **양의 부딪힘 판정에 죽은 식**: `+ TOUCH - 0.55` 인데 TOUCH 가 정확히
  0.55 라 서로 지워진다. 정리했다 (판정 거리는 그대로).
- **넓힌 판의 중거리가 비어 보였다.** ring 구간에서 반지름을 균등
  샘플링하고 있어서 넓이가 제곱으로 느는 바깥쪽보다 안쪽에 쏠렸다
  (`#setProps` — `#scatterRocks` 에 이미 썼던 "제곱에서 뽑기"를 뒤늦게
  같이 적용). 풀·덤불에는 `spec.fill` 을 새로 달아 넓이만큼 늘렸다
  (상한 4.5배, 폴리곤이 가벼워서 드로우콜 값을 치를 만하다).

### 문서가 코드와 어긋나 있던 것 (법적으로 중요했다)
`bosses.js` 의 안티파테스 주석이 "Quaternius CC0 거인으로 바꿨다" 고
적혀 있었는데, 실제 `antiphates.glb` 의 glTF extras 를 열어 보니 여전히
**ribtibs 의 Fire-Branded Ogre (Sketchfab, CC-BY 4.0)** 였다 — 애니메이션
0개, 정적 메시. 주석이 말한 "7 클립 붙은 CC0 거인"은 **다른 파일**
(`giant.glb`, 안티파테스가 부르는 부름꾼의 몸)이었다. 두 몸을 헷갈린
주석이 왕의 `look:` 줄 바로 위에 달려 있었다.

CREDITS.md 에는 적혀 있었지만 **게임 안 일시정지 메뉴 '출처' 화면에는
없던** CC-BY 자산이 셋이었다(스킬라의 촉수·세이렌·이제 밝혀진 안티파테스).
CC-BY 는 표기가 라이선스 조건이라 CREDITS.md 에 적는 것만으로는 부족하다
— 읽는 사람이 그 파일을 안 본다. 셋 다 화면에 추가했다.

> `HANDOFF.md` 자체도 낡아 있었다. "시험용 무적이 기본 켜짐" 이라는
> 4절의 경고는 이미 고쳐진 뒤였고(`q.get('god') === '1'`, 기본 꺼짐,
> 배포 사이트에서 실측 확인), "254ms 긴 작업"은 다시 재 보니 재현이
> 안 됐다 — 일회성 잡음이었을 가능성이 있다. 둘 다 위 4절에서 지운다.

### 개발 도구
`__shot`(화면 캡처)이 미니맵을 못 담았다. `XMLSerializer` 는 `<canvas>`
를 빈 태그로만 옮긴다 — 그려진 픽셀은 DOM 이 아니라서 안 딸려 간다.
캔버스만 따로 제자리에 한 번 더 찍어 준다 (`main.js` 의 `__shot`).

### 크기
미니맵 148 → 224px, 할 일 판 238 → 316px + 청동 판 배경. 판이 34~38
반지름짜리라 점 사이 간격이 손톱만 해서 정보가 아니라 장식이었다.

---

## 1.4 7차 갱신 (2026-09-21 저녁) — 넓힌 판, 지도, 쳐내기

### 판을 넓혔다 (땅 34~38 · 배와 홀 23~30)

반지름 16 은 카메라가 판을 통째로 담는 크기였다 — 싸우는 자리가 곧 판
전체라 자리를 잡을 여지가 없다. 넓히면 따라와야 하는 것이 넷이다:

| | |
|---|---|
| 적이 오는 자리 | 판 가장자리 → **나를 중심으로 22~30 고리** (`spawnEnemy`). 안 고치면 50 걸음 밖에서 생겨 걸어오는 데만 십 초 |
| 소품 수 | 기준 반지름 18 에 비례해 늘린다 (`#setProps` 의 `dense`) |
| 잡석 | 넓이(제곱)를 따라간다. 인스턴스라 드로우콜 하나. 반지름을 **제곱에서 뽑아야** 가장자리가 안 휑하다 |
| 카메라 | 판마다 한 뼘씩 물렸다 |

> **동굴만 23 으로 되돌렸다.** 34 로 키웠더니 벽도 천장도 한 번도 화면에
> 안 들어왔다. 넓은 어둠은 동굴이 아니라 밤의 들판이고, 갇힘은 경계가
> 보여야 성립한다.

### 바닥의 바둑판 — 확률적 타일링

넓히자 바닥에 격자가 떴다. 한 장을 아홉 번 반복해 까는데 그 안에 밝기
기울기가 있으면 이어 붙인 자리마다 밝은 귀와 어두운 귀가 맞닿는다.
**반복수를 줄여도, 노멀 세기를 0 으로 내려도 그대로였다** (둘 다 해 봤다) —
격자는 알베도 자체에 있었다.

`world.js` 의 `#breakTiling` 이 칸마다 읽는 자리를 난수만큼 밀고 뒤집어 네 번
읽고 섞는다 (Inigo Quilez). `textureGrad` 로 미분값을 원래 UV 에서 떠서
넘기는 것이 핵심이다 — 안 그러면 칸 경계마다 흐릿한 선이 한 줄씩 생긴다.

### 미니맵 · 할 일 판

- `ui/minimap.js` — 판 경계(판 모양 그대로)·나·동료·적·보스·전리품·양.
  저승에서는 벽까지. 북쪽 고정 (카메라 yaw 가 0 이라 돌리면 어긋난다).
- `ui/quest.js` — 지금 할 한 가지와 **파훼 한 줄** (`cfg.how`, 보스 일곱에
  다 적었다). 쓰러진 동료가 있으면 그게 맨 위로.
- `core/hangul.js` — '안티파테스을(를)' 을 없앤다. 받침으로 조사를 고른다.

### 저승은 두 배 길게

칸 폭을 5 걸음으로 고정하고 칸 수를 판 크기에서 뽑는다. 굽이도 늘렸다
(0.55 → 0.68). **걸을 거리 70 → 149, 칸 17 → 30.**

### 동굴 — 톤 예외 + 천장

두 가지가 원인이었다. 첫째, 기본 톤('정오의 대리석')이 env 를 전부 덮어써서
**파르테논처럼 밝은 동굴**이었다 (`keepEnv: true` 로 뺐다). 둘째, 어둡게 한다고
동굴이 되지 않는다 — 종유석·석순·키운 바위 벽, 그리고 **입구를 막은 바위**
(이야기가 그 바위로 시작하는데 화면에 없었다).

### 양 — 살아 있는 지형

거인은 목자인데 우리에 양이 없었다. 양은 적이 아니다. 다만 **부딪히면 울고,
그 소리가 거인에게 내 자리를 알려 준다.** 그래서 폴리페모스를 `hunts: true`
로 바꿨다 — 멀면 **마지막으로 알아낸 자리**를 친다 (`Boss.aimPoint`).
알아내는 길은 셋: 7 걸음 안, 그를 때리기, 양의 울음. 이 판에서 처음으로
'어떻게 돌아갈까' 가 판단이 된다.

### 빌려 온 팔 동작을 몸통 기준으로 (활이 T자였다)

활을 당기면 양팔을 옆으로 벌리고 서 있었다. 원본 궁수가 **당기면서 몸을 90도
돌리는데**, 우리는 팔만 옮기니 그 몸통 회전이 팔에 얹힌 것이다. 이제
`retarget.js` 가 월드가 아니라 **몸통 기준**으로 옮긴다 (몸통 뼈는 자동 —
옮기는 뼈 중 부모가 목록에 없는 것의 부모). 칼 셋과 동료의 창질도 같이 고쳐졌다.

### 막기와 쳐내기 (세키로)

방어가 구르기 하나면 모든 예고의 정답이 같아진다. `Q` 를 누르고 있으면
막고(피해 30%, 나머지는 **자세**로), 맞기 직전 0.18초 안에 누르면 **쳐낸다**
(피해 0, 적 무력화 +15). 자세가 다 차면 1.1초 굳는다.
호플론은 새로 짰다 (`gear.js` 의 `buildShieldProp`) — 막는 게 보여야 타이밍
기술이 성립한다.

---

## 1.5 6차 갱신 (2026-09-21) — 동료가 규칙이 되고, 화면이 안 끊기게

### 기세 → 중격 → 무력화 → 그로기

근접전이 **3타를 계속 돌리는 일**이었다. 칼도 활도 기다렸다 크게 치는
길이 있는데 둘 다 공짜라, 언제 무엇을 지를지가 안 물어졌다. 쌓아야만
쓸 수 있는 한 방을 하나 둔다.

| | 어디 |
|---|---|
| **기세** 0–4 | `player/player.js` — 칼이 닿을 때 한 칸, 구르기로 실제 공격을 흘렸을 때 한 칸. 5초 쉬면 한 칸씩 샌다 |
| **중격** `Shift` | `player.js` 의 `HEAVY`. 쌓인 걸 전부 태운다. 칸마다 피해 `22+14n`, 무력화 `16+9n` |
| **무력화** | `enemy/boss.js` 의 `breakPoise`. 다 차면 그로기 + 두 배 피해. 한 번 무너질 때마다 `poiseMax` 가 1.3 배 |
| **함성** `E` | `main.js` 의 `rally`. 동료가 보스를 치는 **유일한** 자리 |

기세 한 칸은 '맞힌 횟수' 가 아니라 '박자' 다 — 한 번 휘둘러 넷을
맞혀도 한 칸이다. `run.gaveFocus` 를 `onActive` 에서 끄는 이유가 이것이다
(`ActionRunner` 는 한 사람당 하나뿐이라 켠 자리에서 안 끄면 영영 켜져 있다).

### 육백 명이 하는 일

전에는 화면 구석의 글씨였다. 이제 두 군데서 힘이다.

- **몇이 곁에 서는가** — 400 이상 셋, 120 이상 둘, 그 아래 하나
  (`companionsFor`). 키코네스에서 일흔둘을 잃으면 곁이 한 칸 빈다.
- **함성이 얼마나 미는가** — `rallyPower(crew)` 가 8~34. 육백이면 한 번에
  보스 무력화를 크게 깎고, 마흔다섯이면 긁는 시늉이다.

그리고 **내가 한 일로도 줄어든다.** 보스의 후려치기·내려찍기 범위에
동료가 있으면 주저앉고(`knockDown`), 곁에 1.6초 서 있으면 일어난다.
12초 안에 못 일으키면 그를 따르던 마흔이 배로 못 돌아온다.

> 적이 동료를 **노리는** 규칙은 일부러 안 넣었다. 노리기 시작하면 동료
> 지키기가 판의 일이 되어 판단이 흐려진다. 장판에 걸리는 것까지만이다.
>
> 동료는 장판을 읽고 반 박자 늦게 비킨다 (`fx.telegraph.dangerAt`).
> 안 그러면 셋이 늘 오디세우스 뒤 한 뼘이라 큰 것 한 번에 통째로 깔린다.
> **단, 함성에 응해 달려가는 동안과 창을 내지르는 중에는 안 비킨다** —
> 부르면 보스 품으로 뛰어드는 것이고, 그게 그 기술의 값이다.

### 화면이 넘어갈 때 삐걱거리던 것 (세 가지)

1. **따라잡기.** `core/loop.js` 가 벌어진 시간을 메우려고 한 프레임에
   시뮬레이션을 다섯 칸씩 돌렸다 — 세상이 83ms 순간이동하고 그 프레임도
   다섯 배로 걸린다. 이제 `real > 0.1` 이면 그 시간을 버린다.
2. **60Hz 를 120Hz 화면에 그대로.** ProMotion 에서는 두 프레임에 한 번씩
   같은 자리가 나와 잘게 떤다. `combat/actor.js` 가 한 칸 전 자리와 지금
   자리를 들고 그 사이를 그린다 (`setRenderAlpha`). 규칙은 60Hz 고정.
   자리를 옮긴 건 거리로 알아채고 붙인다 (`mark()`).
3. **연출 타이머가 그리기에 묶여 있던 것.** 장비가 몸에 붙는 장면이
   `draw()` 안에서 시간을 셌다. 화면을 안 그리는 동안(액자가 덮었을 때,
   탭이 뒤로 갔을 때)에는 시계가 멈춰 **일시정지가 영영 안 풀린다.**
   `Game.drive(real)` 로 빼서 루프의 시계에 붙였다.

그 앞 회차에서 고친 첫 프레임(140.6ms → 2.4ms)은 `world.js` 의
`applyStage` 끝 — `compileAsync` 만으로는 모자라고 막 뒤에서 **한 장 실제로
그려 봐야** 그림자 지도와 후처리 패스가 만들어진다.

---

## 1.6 5차 갱신 (2026-09-21) — 화면이 말을 하게

판을 하나씩 돌려 보면서 **눈에 보이는 것이 틀린 자리**를 찾아 고친 회차.
대부분 새 기능이 아니라 "있는데 안 보이거나, 보이는데 틀린" 것들이었다.

| 무엇 | 어디 | 요점 |
|---|---|---|
| 투구 | `player/player.js` MOUNT.helmetReal | Sketchfab 실물로 교체. 원점이 투구 바닥이라 통째 bbox 로 맞추면 머리 위에 뜬다 — 볏 뺀 몸통과 Head 본 정점을 **재서** 포갰다. 폭만 1.35 배(머리는 넓고 짧은데 투구는 좁고 길쭉) |
| 검은 칼날 | `player/gear.js` steel | metalness 0.95 는 되쏠 환경이 없으면 각도에 따라 새까맣다. 0.55·0.4 로 ([#3](https://github.com/41ways/odyssey/issues/3)) |
| 뒤집힌 활 | `player/player.js` MOUNT.bow | 시위가 바깥을 보고 있었다. 반 바퀴. 당기는 동안 칼은 치운다 |
| 잘리던 보스 | `render/world.js` setBossFocus | 세이렌(키 4.2)이 화면 위로 잘려 꼬리만 보였다. 설 때 키를 재서 시선을 그만큼 올린다 ([#4](https://github.com/41ways/odyssey/issues/4)) |
| 물 위를 걷던 동료 | `stage/sailleg.js` | 항해에서 오디세우스만 감추고 동료를 뒀다 ([#5](https://github.com/41ways/odyssey/issues/5)) |
| 갈 곳 없던 바다 | `stage/sailleg.js`, `render/props.js` buildIsle | 나아간 거리만 세어서 **아무 쪽으로 가도 도착**했다. 섬을 앞뒤로 세우고 도착을 "섬에 닿기"로. 안개·각도·카메라 yaw 세 가지가 함께 걸렸다 |
| 빈 판 | `stage/stages.js` | 가장자리에만 소품이 있고 싸우는 화면은 맨바닥이었다. 숲 판이 이미 쓰던 방식(발밑 채우기)을 이스마로스·텔레필로스·동굴·이타카에 폈다 |
| 이타카의 홀 | `render/props.js` | 연회상과 화로를 코드로 짰다. 방 한가운데는 비운다 — 소품에 충돌이 없기도 하고, 그리스 연회방(안드론)이 원래 그렇다 |

**확인하다 알게 된 것 (중요):** 브라우저 탭이 **백그라운드면 rAF 가 멈춘다.**
"적이 안 나온다", "화면이 멈췄다" 로 보이는 것 대부분이 이것이다. 문서 위쪽에
이미 적혀 있는데 또 걸렸다 — `__tick(n)` 으로 손으로 밀 것.

**남은 것:** 공격 3타가 시각적으로 같다(클립이 `Sword_Attack` 하나뿐, 프레임
데이터는 타마다 다르다). 활 동작 클립은 묶음에 아예 없어 권총 조준을 빌려 쓴다.
저승·죽음 판은 아직 안 훑었다.

---

## 1.7 4차 갱신 (2026-09-18 오후) — 신화 속에 들어와 있게

"RPG 하는 기분, 그리스 신화 안에 들어와 있는 몰입" 을 목표로 한 회차.

| 무엇 | 어디 | 요점 |
|---|---|---|
| 여정 층 | `stage/voyage.js`, `ui/fate.js`, `ui/voyagehud.js` | 배 12·동료 600 이 호메로스의 자리에서 줄어든다. 포세이돈 분노(바다 판 적 맷집)·아테나 호의(은총 한 장 더). 판 사이 갈림길 넷 — 이름 / 몰리 / 한 해 / 헬리오스의 소 |
| 동료 전투원 | `player/companion.js` | 에우릴로코스·폴리테스, 이타카에선 텔레마코스·에우마이오스. 적은 동료를 안 노리고 동료는 보스를 안 친다 (파훼는 플레이어 몫) |
| 옷차림 | `player/gear.js`, `player/player.js` | 알몸·가죽 바지 → 키톤·샌들로 시작, 전리품은 청동 흉갑·팔가리개·견갑·코린토스 투구·망토. 허리 자락(프테루게스)은 골반 위치를 따라간다 |
| 망토 | `player/cloth.js` | 바람 가속이 중력의 10배라 늘 수평이었다 → 서면 15°, 달리면 60° |
| 저승 | `render/world.js` setMaze | 보라 상자 → 층진 바위(crag), 검은 포플러, 아스포델, 바닥 안개 셰이더 |
| 폴리페모스 동작 | `render/retarget.js` | 오디세우스의 공격·조깅·죽음을 바인드 포즈 기준 월드 델타로 옮김 |
| 안티파테스 뼈 | `render/autoskin.js` | 정적 오우거에 영웅 뼈대를 관절 표(`models.js` OGRE)대로 입힘. 영웅 클립 그대로 재생. 라이스트리고네스 잡졸도 같은 몸 |
| 세이렌 크기 | `models.js` | 키 15.7 기둥 → 4.2 ([#2](https://github.com/41ways/odyssey/issues/2)) |
| 스킬라 | `bossparts.js` skyllaKraken | 안 보이던 것 ([#1](https://github.com/41ways/odyssey/issues/1)), 머리마다 제 촉수로 친다 |
| 카리브디스 | `render/vortex.js` | 로그 나선 셰이더 물, 판 전체 수면 |
| 활시위 컷신 | `stage/cuts.js` CUT_BOW | 안티노오스 앞, 거지 차림을 벗기 전 (`cutBefore`) |
| 엔딩 | `ui/hud.js` credits | 여정의 기록 — 어디서 몇이 돌아오지 못했고, 무엇을 골랐는가 |

**갈림길 그림 넷** (`public/img/fate-*.webp`) 은 들어갔다 — 제미나이로 뽑았다.

**투구 (2026-09-21 끝):** 코드 투구를 Sketchfab 'Agamemnon's Helmet'
(Quesho, **CC-BY**) 으로 바꿨다. 30MB·52만 삼각형 원본에서 볏 털을 덜고
5,384 개·230KB 로 줄였다 (`tools/prep-helmet.mjs` → `public/models/helmet.glb`).
붙이면서 걸린 것 —
  · GLB 안 메시가 이미 0.01 배(원본 cm) 라 MOUNT 의 값이 0.009 대다.
    한동안 "보정이 100 배 어긋난다" 고 적혀 있었는데 그게 아니었다.
  · 원점이 투구 바닥이고 볏이 위로 길어서 통째 bbox 로 맞추면 머리 위에 뜬다.
    볏 뺀 몸통과 Head 본에 매달린 정점(=머리)을 머리 좌표계에서 재서 포갰다.
  · 원본 재질(metalness 1)과 압축된 색 텍스처(평균이 거의 순검정)가 겹쳐
    새까맣게 나왔다. 텍스처를 버리고 다른 청동과 같은 값으로 칠한다.
CC-BY 표기는 멈춤 메뉴(Esc) 의 **출처** 항목에 있다 (`ui/menu.js` CC_BY).
새 CC-BY 에셋을 넣으면 거기에 한 줄 더한다 — **법적으로 필요**.

**Mixamo:** 안티파테스 OBJ(170cm·정점 붙임)로 세 번 올렸는데 서버 리깅이
`Unknown error while generating motion` 으로 떨어졌다. 그래서 autoskin 으로 갔다.
`tools/glb-to-obj.mjs`, `tools/web/fbx2glb.html` 은 다른 모델에 쓸 수 있게 남겨 둔다.

**그림 옮기는 길:** 생성기 탭에서 연속 다운로드가 막히면, 그림을 화면에 띄워
스크린샷을 찍고 `http://localhost:5180/tools/web/upload.html` 의 파일 입력으로
올린다 → `shots/` 에 떨어진다 (`tools/prep-bow.mjs` 참고).

---

## 2. 이번 회차에 끝낸 것

### 레벨·경험치 (`src/player/level.js`) — 새로
성장 선택지에 계열(칼·활·발)과 등급(일반→레어→유니크→레전더리)이 이미
있었는데, 윗등급이 같은 계열 2·4·6 번을 요구하는 데 반해 **고를 기회가
게임 전체에서 여덟 번**(보스 은총 7 + 유물 1)뿐이었다. 레전더리는 만들어
놓고 닫아 둔 문이었다.

`need(L) = 24 + 13(L-1)`. 선형이라 **판마다 두 번** 오른다. 은총을 더해
판당 세 번, 여덟 판이면 스무 번 — 스무 번 중 여섯이면 한 계열에 30%다.
실제 `stages.js` 로 돌려 확인한 결과:

| 판 | 얻는 경험치 | 레벨 |
|---|---|---|
| 이스마로스 | 80 | → 3 (+2) |
| 텔레필로스 | 113 | → 5 (+2) |
| 아이아이에섬 | 138 | → 6 (+1) |
| 세이렌의 바다 | 180 | → 8 (+2) |
| 메시나 해협 | 186 | → 10 (+2) |
| 이타카 | 396 | → 12 (+2) |
| 죽음 | 56 | → 13 (+1) |

경험치와 적 체력은 둘 다 깊이에 따라 붙는다 (경험치 +26%/판,
잡졸 체력 +24%/판, 보스 +14%/판). 내 화력이 판마다 세 번씩 오르는데
적이 그대로면 뒷판이 헐거워지는 게 아니라 **무의미해진다**.

### 전투 속도 (`src/player/player.js`)
칼이 너무 빨라서 활과 차징이 의미가 없던 문제. 1·2타 회복을 늘리고
3타를 크게(36피해, 그로기 0.48), 활은 만작 보상을 키웠다(7 → 34).
측정값: 칼 3타 연계 1.99초 / 65피해 = **32.7dps**, 활 탭 7dps,
활 만작 21.3dps + 관통, 그로기 중 칼 78.4dps. 구르기 충전 2.5 → 3.4초.

### 보스마다 다른 파훼
전에는 폴리페모스(눈)와 카리브디스(테두리 이빨)만 제 파훼가 있었다.

| 보스 | 파훼 | 어떤 동작을 요구하는가 |
|---|---|---|
| 폴리페모스 | 눕히고 눈을 활로 | 약점 — 정확히 한 점 |
| 안티파테스 | **부름꾼을 먼저 치운다** | 표적 — 시선을 나누기 |
| 키르케 | **잔을 끊는다** (2.4초 시전 중에) | 시점 — 공격 **전**에 들어가기 |
| 세이렌 | **등 뒤로 돌아간다** (정면 반쪽이 다 위험) | 방향 |
| 스킬라 | **머리를 끊는다** (회복 구간에만) | 시점 — 공격 **뒤**를 치기 |
| 카리브디스 | 테두리 이빨 여덟 개를 깨기 | 환경 |
| 안티노오스 | **잔을 들 때 활을 꽉 당겨 쏜다** | 거리 — 벌리고 기다리기 |

일곱 보스가 일곱 가지 다른 동작을 요구한다. 새 장치 네 개가
`src/enemy/boss.js` 에 데이터로 들어갔고, 다른 보스에도 한 줄로 붙는다:
- `guarded: true` + `summon({ guards: true })` — 부름꾼이 살아 있으면
  몸이 안 열린다. 왕과 부름꾼을 초록 고리로 잇는다.
- `mend({ heal, breakSay, breakBy })` — 안 끊으면 회복하는 시전.
  `breakBy: 'draw'` 면 **꽉 당긴 화살만** 끊는다 (`cfg.bowMin`).
  `breakGroggy` 로 끊었을 때의 반격 창을 준다.
- `gaze({ halfAngle, slowFor })` — 정면 부채꼴만 위험하고 등 뒤가 안전.
- `heads: N` + 패턴에 `head: n` — 그 패턴의 **회복 구간**에 때리면 머리가
  끊기고, 끊긴 패턴은 판에서 사라진다 (`severMult`, `severGroggy`).

두 가지를 실측해서 맞췄다 —
· 스킬라: 3타(36피해)로 여섯 번 끊으면 388.8 = 체력의 46%. 파훼가 싸움의
  절반이다. 처음에는 끊는 타격에 그로기 배수가 겹쳐 4.4배가 됐고,
  그러면 네 번째 머리에서 보스가 죽었다 — 그로기를 타격 뒤로 미뤘다.
· 안티노오스: 처음에는 **늘** 화살만 받게 짰다. 그러면 칼을 키운 사람은
  마지막 판에서 빌드가 통째로 무효가 된다. 잔을 든 동안만 닫히고,
  열면 4초 그로기라 그 뒤는 자기 빌드로 몰아치게 바꿨다.
  평상시 피해는 칼·탭사격 다 정상(측정 확인).

### 컷신 그림 배정
컷신 다섯 중 넷이 액자(박물관) 그림을 아무거나 끌어다 쓰고 있었다.
동굴 컷신 첫 장의 글은 "동굴에는 양 떼와 치즈가 있었다" 인데 그림은
폭풍우 치는 갑판이었다. 있는 그림 스물여덟 장을 실제로 보고 다시
배정했다. 규칙 두 개를 `src/stage/cuts.js` 맨 위에 적어 뒀다.

### 효과음 (`src/core/sfx.js`) — 새로, 파일 0 바이트
이 게임에는 배경 음악만 있었다. 그래서 칼이 몸에 들어가는 순간과 허공을
가르는 순간이 **소리로 구분되지 않았다.** 프레임 데이터를 아무리 다듬어도
맞았는지 빗맞았는지를 귀로 모르면 타격감이 안 생긴다.

파일을 받아 오는 대신 합성했다 — 필요한 소리가 전부 잡음 한 조각과
사인파 몇 개로 되는 것들이었다. 칼바람은 필터 쓸어내린 잡음, 타격은
낮은 쿵 + 짧은 잡음, 금속은 어긋나게 맞춘 사인파 셋.

열다섯 종을 넣고 **실제 파형을 재서** 크기를 맞췄다 (AnalyserNode):
피격 0.23 > 쿵 0.24 > 발사 0.13 > 만작 0.06 > 발소리 0.006.
중요한 소리가 크고 잔소리가 작다. 클리핑 없음(전부 1.0 미만).

두 가지를 실측으로 고쳤다 —
· 활 당기는 소리를 gain 0.14 로 뒀는데 재 보니 정점이 0.011 이었다.
  좁은 밴드패스(Q 2.4)가 잡음을 거의 다 깎아 낸 것이다. **필터 뒤의**
  크기를 보고 정해야 한다. Q 를 1.6 으로 열고 gain 을 0.7 로 올려 0.057.
· 내가 맞는 소리를 적이 맞는 소리와 같은 칸에 두면, 적 다섯을 베는 중에
  내가 맞았을 때 간격 제한에 걸려 사라진다. 제일 중요한 소리를 잃는
  자리라서 `thud` 로 따로 뺐다.

만작에 닿는 프레임에 '띵'(`ready`)을 넣은 게 차징 문제의 실제 해답이다 —
조준하는 동안은 화살촉 색이 바뀌는 걸 못 보니, 놓을 때를 귀가 알려 줘야
차징이 기다림이 아니라 기술이 된다.

M 음소거는 음악과 같이 간다 (측정 확인: 켜짐 0.223 → 음소거 0 → 다시 0.233).

### 읽히게 고친 것 — 한글에 라틴 제목 어법을 쓰고 있었다
판 이름이 "이 스 마 로 스" 로 흩어져 있었다. 글자 사이를 `.28em` 벌려
뒀는데 그건 라틴 대문자 제목의 어법이고, 한글은 글자 자체가 네모라서
그만큼 벌리면 읽는 게 아니라 글자를 세게 된다. **한글의 상한은
0.02~0.06em 이다.** 판 이름·설명·웨이브 줄·보스 이름·약점 안내·누적 피해를
다 좁히고 키우고 밝혔고, **글자 뒤에 판을 깔았다** (움직이는 3D 위의 작은
갈색 글씨는 배경이 밝아지는 순간 사라진다).

> 새 UI 글을 쓸 때 이 규칙을 지켜야 한다. 한글에 `letter-spacing`
> 0.1em 이상은 제목이 아니라 낱글자가 된다.

### 알림 층 (`src/ui/notice.js`) — 새로, 로스트아크 차용
얻는 순간이 다 조용했다. 세 겹으로 나눴다 —
1. **가운데 큰 알림** (레벨업·해금): 등급 색 광선이 퍼지고 빛 한 줄이
   글자를 스친다. 판은 안 멈추고 스스로 지나간다. 큐를 두어 겹치지 않는다.
2. **오른쪽 획득 줄**: 성장·장비를 쌓는다. 다섯 장 넘으면 오래된 것부터
   내린다. 카드 화면은 닫히면 사라지므로, 무엇을 모으고 있는지가 화면에
   남아야 빌드가 보인다.
3. **등급이 오를수록 연출을 키운다**: 레전더리만 한 번 반짝인다.
   플레이어가 등급을 글자 대신 밝기로 읽는다.

제일 값이 큰 건 **등급 해금 알림**이다. 같은 계열을 두 번 고르면 레어가,
네 번이면 유니크가 열리는데 그게 다음 선택지에 조용히 한 장 더 생기는
것으로만 표현됐다 — 열린 걸 안 알려 주면 계열을 모으는 게 선택이 아니라
우연이 된다.

### 신화 고증
· **텔레필로스에 거인이 없었다.** 거인족의 마을인데 잡졸이 사람 크기
  키코네스족이고 거인은 왕 하나뿐이었다. 라이스트리고네스 잡졸을 만들고
  (왕과 같은 모델, 키 4.6 → 2.9) **바위를 던지게** 했다 — 열한 척을
  가라앉힌 건 왕의 창이 아니라 온 마을이 절벽에서 던진 돌이다.
  그래서 이 판만 리듬이 다르다: 몰려오는 걸 베는 판이 아니라 하늘에서
  떨어지는 자리를 보고 피하며 거리를 좁히는 판.
· **안티노오스와 텔레고노스가 똑같이 생겼다.** 공용 사람 몸에 색만 달랐다.
  왕(금·청동 귀족) / 후드를 쓴 자 로 갈랐다 — 둘 다 같은 모듈러 팩이라
  뼈대가 같고 Sword_Slash·Roll·Death 를 포함한 클립 스물넷을 공유한다.
· **컷신 13 → 30 컷.** 컷마다 `focus: [x%, y%, 배수]` 로 화각을 준다.
  같은 그림을 넓게/당겨/더 당겨 쓰면 카메라가 장면 안을 움직인 것이 된다.
  돌을 던지는 장면(CUT_TELEPYLOS)을 새로 넣었다 — 열한 척이 가라앉는
  장면인데 컷신이 아예 없었다.

### 정예 (`src/enemy/elite.js`) — 새로
웨이브가 하는 일이 "같은 적을 수만 늘려 보내는 것" 이었다. 그러면 판이
어려워지는 게 아니라 길어질 뿐이고, 플레이어의 판단도 늘 같다 — 가까운
것부터 벤다. 무리 안에 **다르게 싸워야 하는 한 마리**를 섞으면 표적 순서가
판단이 된다 (로스트아크의 네임드, 몬헌의 특수개체).

| 특성 | 무엇이 다른가 | 답 |
|---|---|---|
| 청동을 두른 | 화살이 32% 만 박힌다 | **붙어야** 한다 |
| 광포한 | 행동이 1.5 배 빠르다 | **떨어져야** 한다 |
| 역병을 진 | 죽은 자리가 5초 불탄다 | **어디서 죽일지** 고른다 |

앞의 둘이 정반대인 게 핵심이다. 둘이 같이 나오면 붙어야 하는 놈과
떨어져야 하는 놈을 동시에 상대한다.

체력 ×2.6 · 덩치 ×1.18 · 경험치 ×3. 빈도는 판 깊이에 따라 0.06 → 0.27.
발밑에 특성 색 고리가 깔린다 — 쿼터뷰에서는 몸 색이 조명에 묻히지만
바닥 고리는 내려보는 각도라 늘 보인다.

곁들여 **바닥 불** 하나가 들어갔다 (`Game.leaveFire` / `#tickHazards`).
적은 안 태운다 — 적까지 태우면 불을 깔아 주는 게 이득이 되어 위험이
아니라 보상이 된다. 구르기 무적은 불에도 통한다.

### 리소스 교체 (CSS·코드 모델링 걷어내기)
"css로 모델링한 게 너무 많다" 는 지적을 받은 자리들. 받아 온 건 다 CC0 다.

| 자리 | 전 | 후 |
|---|---|---|
| 판 바닥 여덟 | 색(map)만 | + 노멀맵 · 거칠기맵 (ambientCG) |
| 스킬라 몸 | 정적 오로치(머리 8개) | 애니메이션 촉수 6 (Quaternius) |
| 카리브디스 팔 | 코드 원통 마디 | 같은 촉수 6, 살색으로 |
| 잡석 110개 | 12면 다면체 | 받아 온 바위 지오메트리 (드로우콜 그대로 1) |
| 안티파테스 | 사람 몸을 4.6m 로 늘린 것 | 애니메이션 거인 7클립 (Quaternius) |

**스킬라가 제일 컸다.** 오로치는 정적 메시라 목이 안 움직였고 (내려찍는
보스인데 몸이 안 움직이면 언제 때릴지를 못 읽는다) 머리가 여덟이었다.
그리고 제 모델이 붙은 보스는 코드 조각을 안 받으므로 (boss.js 의
`buildBossBody`), 공들여 만든 여섯 머리 조각이 **여태 화면에 나온 적이
없는 죽은 코드**였다. 몸을 버리고(hideBody) 촉수 여섯을 몸으로 삼았다.
끊긴 머리는 `severed` 를 보고 사라진다 — 3절에 적어 뒀던 '연출이 없다'
항목이 이걸로 닫혔다.

측정해서 고친 것 —
· 바닥 노멀 두 장이 사실상 평평했다 (표준편차 0.6 · 0.8). 매끈한 재질은
  노멀맵이 없는 것과 같다. 홈 있는 널판·줄눈 있는 포석으로 갈았다.
· 촉수 파일은 **바인드 포즈와 애니메이션 트랙의 단위가 어긋나 있다.**
  바인드 포즈 0.197 인데 애니메이션이 뼈를 20 단위 밖으로 보낸다.
  그래서 가만히 있으면 맞고 움직이면 촉수 하나가 투기장을 덮었다
  (실측 125×66×81 → 지금 1.7×3.3×3.1). `MANIFEST.scale` 로 숫자를 박는다.
· 촉수 기울기는 끝 뼈의 월드 높이를 재서 골랐다 (0.28 → 머리 y 4.1,
  화면 위로 지나가 위협이 안 된다 / 0.9 → y 1.2, 가슴 높이).

### 그 밖에
판 모양 일곱 가지(`arena.js`, 윤곽 오차 ≤ 3.6e-15, 경계 누출 0),
저승 미로, 소용돌이(테두리 이빨), 배경 음악 네 곡 + 크로스페이드,
커서 네 종, HUD 키 글리프, 망토 verlet 천(9×11 = 99 정점),
돼지 돌진 / 늑대 도약 / 사자 포효 둔화.

---

## 3. 지금 바로 집을 수 있는 일 (값이 큰 순서)

### (1) ~~컷신 그림을 실제로 만든다~~ → 9차 갱신에서 끝. 세 장 다 갈았다
(동굴 문 막기 · 저승 내부 · 스킬라 절벽. `src/stage/cuts.js` 참고)

**다음에 또 그림이 필요하면 이렇게 한다** (제미나이 무료 계정, 실제로
써서 검증한 절차):

1. 크롬에서 `https://gemini.google.com/images`, 가로세로 비율 **16:9**로
   맞추고 프롬프트를 넣어 만든다.
2. **그림을 클릭해서 확대 뷰(편집기)로 들어간 다음, 오른쪽 위 다운로드
   아이콘(⬇)을 누른다.** `~/Downloads/Gemini_Generated_Image_*.png` 로
   떨어진다 — 브라우저의 진짜 다운로드라 CSP 도, blob 수명도, 새로고침도
   신경 쓸 게 없다.
3. `art/gen/` 으로 옮기고 `cwebp -q 82 -resize 1408 0 원본.png -o
   public/img/이름.webp` 로 줄인다 (기존 `lude-*` 가로 그림과 같은 폭).
4. `src/stage/cuts.js` 의 `art:` 를 바꾸고, `focus` 좌표를 그림의 실제
   구도에 맞게 다시 잡는다. **`Tab` 패널로 그 컷을 직접 틀어서** 글과
   화면이 맞는지 확인한다 — hold 가 1.5~2초라 눈으로 스치듯 지나가므로,
   놓치면 `python3` 로 `focus` 좌표대로 원본을 잘라서 확인하는 편이 더
   정확하다 (`crop((cx*w-cw/2, cy*h-ch/2, ...))`).

> **아래 postMessage/`/__drop` 절차는 이번 회차에 다시 시도했으나
> 막혔다** (구글 페이지가 다시 로드되며 포커스를 잃었는지 iframe 이
> 깨진 이미지 아이콘만 보였고, postMessage 응답이 안 왔다). 정확한
> 원인은 못 찾았지만 위 ⬇ 버튼 방식이 훨씬 간단하고 실패하지 않으니
> **이제 이 절차 대신 위 방식을 쓴다.** 예전 기록을 남겨만 둔다 —
> 언젠가 대량으로 뽑아야 할 때는 자동화가 다시 필요할 수 있다.

1. ~~크롬에서 `https://gemini.google.com/images`, 가로세로 비율 **16:9**.~~
2. ~~`npm run dev` 를 띄워 둔 채, 제미나이 탭 콘솔에서:~~
   ```js
   const f = document.createElement('iframe')
   f.id = '__drop'; f.src = 'http://localhost:5180/drop.html'
   f.style.cssText = 'position:fixed;right:8px;bottom:8px;width:360px;height:220px;z-index:99999'
   document.body.appendChild(f)
   ```
3. ~~그림을 만든 **직후에** (새로고침하기 전에) 같은 콘솔에서:~~
   ```js
   const im = [...document.querySelectorAll('img')]
     .filter(i => i.src.startsWith('blob:') && i.naturalWidth > 600).pop()
   const c = document.createElement('canvas')
   c.width = im.naturalWidth; c.height = im.naturalHeight
   c.getContext('2d').drawImage(im, 0, 0)
   c.toBlob(b => f.contentWindow.postMessage(
     { name: 'cave-02.png', blob: b }, 'http://localhost:5180'))
   ```
4. ~~`art/gen/` 에 떨어진다. `public/img/` 로 옮기고 webp 로 줄인다.~~

> **왜 이렇게 돌았었나.** 세 가지가 다 막혀 있었다. ① 한 탭에서 두 장 이상
> 자동 내려받기를 하면 크롬이 그 사이트를 막고, 막히면 사용자가 손으로
> 풀어야 한다. ② 구글 페이지의 CSP(`connect-src`)가 localhost 로 가는
> `fetch` 를 끊는다 — 서버에서 CORS 를 다 열고 사설망 헤더까지 붙여도
> 안 뚫린다. 막는 쪽이 서버가 아니라 보내는 페이지다. ③ 새로고침하면
> 그림이 `blob:` 에서 `googleusercontent` 주소로 바뀌는데 그건 CORS 가
> 없어서 캔버스가 오염되고(`tainted canvas`) 바이트를 못 꺼낸다.
> 그래서 **만든 직후 blob 에서 꺼내** `postMessage` 로 넘긴다 —
> postMessage 는 네트워크 요청이 아니라 CSP 가 볼 것이 없다.

새 그림이 오면 `src/stage/cuts.js` 의 `art:` 만 바꾸면 된다. 세로로 긴
그림은 자동으로 `.plate.tall` 이 붙는다 (`/boss/`, `/rise/` 경로 기준).

### (2) 남은 코드 모델링 — 세이렌의 날개, 안티파테스의 덩치
스킬라·카리브디스는 받아 온 촉수로 갈았다. 남은 자리는 둘이다.
- **세이렌**은 사람 몸에 코드로 만든 날개를 달고 있다 (`sirenWings`).
  하피/새사람 모델이 있으면 그대로 바뀐다. poly.pizza 에서 harpy·bird
  로 찾아보면 후보가 있다 (검색 → uuid → `static.poly.pizza/<uuid>.glb.br`,
  아래 6절에 절차를 적어 뒀다).
- ~~**안티파테스**는 사람 몸을 4.6 미터로 늘린 것이다~~ → **해결.**
  받아 온 거인으로 갈았다 (실측 키 4.55). 실루엣이 사람과 갈린다.

### (3) 크레딧 화면에 CC-BY 표기를 넣는다 ★ 법적으로 필요
`CREDITS.md` 에는 적어 뒀지만 **게임 안 크레딧 화면에는 아직 없다**.
Sketchfab CC-BY 두 건(Cyclops Rig / DM-913, Yamata no Orochi / tran95)은
표기가 라이선스 조건이다. `src/ui/hud.js` 의 `credits()` 에 한 줄 블록을
더하면 된다. 배포 전에 반드시.

### (4) ~~시작 화면 → 난이도 전환의 254ms 긴 작업~~ → 재현 안 됨
`PerformanceObserver` 로 한 번 잡혔던 123~254ms 멈춤을 8차 갱신에서
다시 재 봤는데 **똑같은 순서로도 안 잡혔다.** 이 게임 코드가 원인이
아니라 그 순간의 브라우저/확장 잡음이었을 가능성이 있다. 다시 보인다면
그때 잡는다.

---

## 4. 알려진 한계 (고친 게 아니라 아는 것)

- ~~`orochi.glb`(스킬라)는 정적 메시다~~ → **해결.** 몸을 애니메이션 촉수
  여섯으로 바꿨다. 파일은 남겨 뒀지만 지금 아무도 안 쓴다.
- **`cyclops.glb` 에 Idle 클립 하나뿐이다.** 걷기·공격 동작이 없어서
  `anims.glb` 의 사람 동작을 빌려 쓰거나 코드로 흔든다.
- **세이렌은 아직 코드 조각 몸이다** (사람 몸 + 코드 날개).
  카리브디스는 팔만 받아 온 촉수로 갈았고, 깔때기·물살·눈은 그대로
  코드다 — 그건 모양이 아니라 움직임이라 코드가 맞다.
- **사자 모델이 없다.** 늑대 몸에 황금 색조(`#d8a850`)로 대역을 쓴다 —
  크기와 색이 달라 실루엣으로는 구분되지만 사자는 아니다.
- 빌드 산출물 16MB (그림 2.5 / 모델 4.1 / 텍스처 4.1 / 음원 3.5).
  노멀·거칠기 16 장으로 3MB 늘었다. README 에 한때 "1.3MB" 라고
  적혀 있었는데 그건 근거 없는 숫자였고, 지금 숫자는 목표가 아니라
  **현황**이다. 줄일 자리는 텍스처를 512 로 내리는 쪽이다 (1024 → 512 면
  노멀이 4 분의 1 이 되는데, 바닥은 반복 타일이라 티가 덜 난다).

---

## 5. 찾아 주면 좋을 것 (사용자에게)

### 그림 — 우선순위대로
~~1. 폴리페모스가 동굴 문을 바위로 막는 장면.~~
~~2. 저승 내부.~~
~~3. 절벽에 붙은 스킬라.~~
→ 셋 다 9차 갱신에서 만들어 끼웠다. 지금 남은 건:
1. 바닥 텍스처의 **노멀맵**. 바닥 그림 자체는 판마다 따로 있다
   (`public/textures/` 여덟 장). 그런데 색(map)만 있고 노멀이 없어서
   빛이 스쳐도 바닥이 납작하다 — 같은 여덟 장의 노멀맵만 있으면
   돌바닥이 돌처럼 읽힌다.

### 3D 모델 — 있으면 판이 달라진다
1. **사자** (rigged, Attack/Walk/Idle). 지금 늑대에 금색을 칠한 대역이다.
   poly.pizza 에는 정적 사자만 있었다 (Poly by Google, CC-BY). 움직이는
   사자는 Quaternius 의 *Ultimate Animated Animals* 팩(CC0, 12종 ×12클립)
   에 있는데 **배포가 구글 드라이브 폴더**라 자동으로 받기가 어려웠다.
   사람이 한 번 받아서 `art/gen/` 에 넣어 주면 그 뒤는 자동이다:
   https://quaternius.com/packs/ultimateanimatedanimals.html
2. **여자 몸 rigged** (키르케·세이렌). 지금은 남자 몸에 후드를 씌운다.
   Quaternius *Ultimate Modular Women* (CC0) — 같은 드라이브 문제.
3. **하피 / 새사람** — 세이렌. 지금 코드 날개다.
4. ~~거인 — 안티파테스~~ → 받았다 (Quaternius Giant, CC0).
   더 정교한 게 있으면 좋지만 급하지 않다 — 지금 것은 각진 저폴리라
   사람 캐릭터와 결이 조금 다르다.
5. 키클롭스 **공격 동작** (Attack / Walk / Stagger). 모델은 있고 동작만 없다.
   Quaternius *Universal Animation Library* (CC0) 가 후보인데 뼈대 호환을
   확인해야 한다 (이 저장소는 UE 식 뼈 이름을 쓴다).

### 음원
효과음은 **파일 없이 합성해서 넣었다** (`src/core/sfx.js`, 0 바이트).
칼바람·타격·급소·금속·활 당김·만작·발사·구르기·쿵·종·레벨업 열다섯 종.
그래서 급하지는 않은데, 아래는 있으면 확실히 나아진다:

1. **몸에 칼이 들어가는 소리**와 **금속이 갑옷에 튕기는 소리**의 실물 녹음.
   합성으로 만든 '퍽' 은 쓸 만하지만 살과 쇠의 차이까지는 안 난다.
   (CC0: freesound, kenney.nl 의 impact 묶음)
2. 보스마다 다른 곡. 지금 보스 곡이 하나라 일곱 보스가 같은 결이다.
3. 짧은 **스팅어** — 판 시작, 보스 등장, 토벌. 2~4초짜리.
   레벨업 스팅어는 합성으로 들어가 있다.
4. **발소리**는 소리는 만들어 뒀는데(`sfx.step()`) 아직 아무 데서도
   안 부른다. 걸음 주기를 잡아 줄 자리가 필요하다 (`Player.#visual` 의
   `_run` 을 쓰면 된다).

### 정해 주면 좋을 것 (메커니즘 판단)
1. **판당 길이.** 지금 잡졸 목표가 14~30마리고, 한 판이 대략 3~5분이다.
   여덟 판이면 한 회차 30~40분. 이게 맞는 길이인가? 짧게 하려면
   `stages.js` 의 `goal` 만 내리면 되는데, 그러면 레벨 곡선도 같이
   조여야 한다 (판마다 두 번이 깨진다).
2. **죽으면 어디로 가는가.** 지금은 `R` 로 처음부터다. 로그라이크라면
   죽음에 남는 것이 있어야 하는데(영구 성장, 해금) 그게 없다.
   넣을 것인가, 한 회차 완결로 갈 것인가.
3. **유물(저승 보상)을 몇 개까지 쌓게 할 것인가.** 지금 한 번에 하나다.

---

## 6. 리소스 받는 절차 (다음 사람이 그대로 쓰게)

### poly.pizza — CC0/CC-BY 모델, 개별 GLB
사이트에 API 는 없지만 **검색 페이지 HTML 에 uuid 가 박혀 있다.** 그래서
브라우저에서 한 번 긁고 셸에서 받는 게 제일 빠르다.

1. 검색 페이지(`poly.pizza/search/<말>`)에서 콘솔에:
   ```js
   [...document.querySelectorAll('a[href^="/m/"]')].map(a=>{
     const c=a.closest('div')??a
     const s=(c.querySelector('img')||a.querySelector('img'))?.getAttribute('src')||''
     const u=(s.match(/[0-9a-f-]{36}/)||[])[0]
     const t=(c.innerText||'').trim().split('
').filter(Boolean)
     return u?{u,n:t[0],by:t[1]}:null
   }).filter(Boolean)
   ```
2. 셸에서 (브로틀리라 `--compressed` 가 필요하다):
   ```bash
   curl -sL --compressed -o out.glb      "https://static.poly.pizza/<uuid>.glb.br" -H "Referer: https://poly.pizza/"
   ```
3. `node tools/prep-gen.mjs out.glb public/models/이름.glb --tex 512`
4. `src/render/models.js` 의 `MANIFEST` 에 한 줄.

> 주의: 썸네일 주소의 uuid 가 파일 이름이다. 페이지 주소의 `/m/<id>` 는
> 다른 값이고 그걸로는 403 이 온다.

### ambientCG — CC0 텍스처 (노멀·거칠기 포함)
API 가 있어서 브라우저가 아예 필요 없다.
```bash
curl -s "https://ambientcg.com/api/v2/full_json?type=Material&q=snow&limit=5"
curl -sL -o m.zip "https://ambientcg.com/get?file=Snow015_1K-JPG.zip"
```
zip 안에 `_Color` `_NormalGL` `_NormalDX` `_Roughness` `_Displacement` 가 있다.
**NormalGL 을 쓴다** — DX 는 초록 채널이 뒤집혀 있어서 요철이 반대로 파인다.
받은 다음 **표준편차를 재서** 평평한 건 버린다 (위 2절 참고).

### 제미나이 — 컷신 그림
3절 (1) 에 콘솔 코드까지 적어 뒀다.

## 7. 구조

```
src/
  core/      loop.js(고정 60Hz) input.js math.js music.js
  combat/    action.js(프레임 데이터) actor.js hit.js projectile.js pickup.js trail.js
  player/    player.js stats.js level.js gear.js blessings.js cloth.js(verlet 천)
  enemy/     boss.js(패턴 장치) bosses.js(보스 도감) bossparts.js kikones.js shade.js
  stage/     stages.js(판 데이터) arena.js(판 모양) run.js waves.js
             cuts.js(컷신) maze.js(저승) maelstrom.js(소용돌이)
  render/    world.js fx.js character.js figure.js models.js particles.js looks.js
  ui/        hud.js levelup.js interlude.js(액자) reel.js(컷신) reach.js
             title.js difficulty.js blessing.js relic.js equipcard.js stagepicker.js theme.js
tools/       prep-gen.mjs   # 받아 온 glb 를 줄인다 (draco, 텍스처, simplify)
art/gen/     받아 온 원본. 빌드에 안 들어간다
public/drop.html             # 제미나이 → 이 저장소 그림 통로 (개발용)
```

**손대기 전에 읽을 것 두 개** —
- `src/stage/arena.js` — 판 경계는 여기 하나가 정한다. 다른 데서
  `Math.hypot(x,z) > R` 를 다시 쓰면 보이는 벽과 안 보이는 벽이 어긋난다.
- `src/combat/action.js` — 적과 보스와 플레이어가 같은 장치를 쓴다.
  시전이 시작되면 위치와 방향이 잠기고, 판정은 잠긴 자리에서 본다.
  보스라고 예외를 두면 "분명히 피했는데 맞았다" 가 된다.

---

## 8. 이 회차에 실제로 밟은 함정 (다시 밟지 않게)

- `attachTo(bone, obj)` 는 뼈의 월드 스케일을 보정한다. 그래서 자식은
  **월드 단위**다. 거인에게 붙일 때 사람 크기로 주면 눈이 콩알이 된다 —
  `look.height / 1.8` 을 곱한다.
- `Actor.sync()` 가 매 프레임 `group.position.copy(this.pos)` 를 한다.
  공중에 띄우려고 y 를 직접 넣으면 지워진다. `e.hopY` 에 넣고
  `super.sync()` **뒤에** 더한다.
- CSS `filter` 는 `clip-path` **보다 먼저** 걸린다. 그래서 자른 모서리를
  흐리려면 자르는 요소와 흐리는 요소를 나눠야 한다.
- `style="...url(\"...\")"` — 쌍따옴표 속성 안에 쌍따옴표를 쓰면 속성이
  거기서 끝난다. `url('...')` 로 쓴다.
- `ActionRunner.stop()` 이 `def` 를 비운다. 멈춘 뒤에 `def.xxx` 를 읽으면
  죽는다 — 먼저 꺼내 두고 멈춘다.
- `camera.project()` 는 절두체 밖의 점에도 좌표를 준다. 화면 밖 피해
  숫자가 구석에 달라붙던 원인이다. 절두체 검사를 따로 해야 한다.
- 적의 `keepRange` 가 좁으면 거리 조건이 붙은 기술이 **한 번도 안 나간다**.
  돼지 돌진이 `d > 3.6` 인데 `keepRange` 가 `[1.6, 2.2]` 라 조건이 안
  열렸다. 기술을 넣어도 안 나가면 없는 것이다.
- 망토 바람은 `-z` 다. 캐릭터의 앞이 `+z` 라서 그렇다 (`player.#moveBy`).
- **전체 화면 선택은 겹칠 수 있다.** 보스를 잡으면 경험치로 레벨이 오르면서
  은총도 같이 뜬다 — 둘 다 `#freeze()` 하고 `await` 하므로 하나를 고르면
  다른 하나가 영원히 기다리고 판이 멈춘다 (세이렌에서 걸렸다).
  새 전체 화면을 붙일 때는 반드시 `#modal()` 을 지나게 한다.
- `attachBossParts` 가 mount 에 `look.height / 1.8` 을 곱한다. 조각의
  좌표는 **원하는 월드 값의 절반**을 적어야 한다 (스킬라에서 ±6.6 이
  월드 ±13 이 되어 투기장 밖으로 나갔다).
- **가려진 탭에서는 CSS 애니메이션 타임라인이 멈춘다** (`visibilityState`).
  요소는 만들어지고 클래스도 붙는데 `opacity` 가 0 에서 안 움직인다.
  확인할 때는 `animation:none` 으로 정지 상태를 박아서 봐야 한다.
- `actor.js` 가 **매 프레임 `emissive` 를 덮어쓴다** (피격 번쩍임·화상).
  거기에 색을 넣으면 다음 프레임에 지워진다 — 지속되는 색은 `color` 로.
- `SkeletonUtils.clone` 은 뼈대와 메시는 복제하지만 **재질은 참조로
  공유한다.** 한 마리를 칠하면 그 종 전부가 물든다. 복제해서 칠하고
  `bodyMats` 도 복제본으로 갈아 끼워야 피격 번쩍임이 맞는 재질을 본다.
- 받아 온 glb 중에 **바인드 포즈와 애니메이션 트랙의 단위가 어긋난 것**이
  있다 (촉수). 바인드 포즈로 잰 배수가 100 배쯤 틀려서, 가만히 있으면
  맞고 움직이면 모델이 판을 덮는다. `MANIFEST.scale` 로 숫자를 박는다.
- **톤 시안(LOOKS)은 판의 env 를 통째로 덮어쓴다.** 어둠이 내용인 곳
  (동굴·저승)은 `keepEnv: true` 로 빼야 한다. 안 그러면 아무리 어둡게 적어도
  화면에 한 번도 안 닿는다 — 실제로 동굴이 파르테논처럼 밝았다.
- **팔만 리타깃할 때는 몸통 기준으로 옮긴다.** 원본이 동작 중에 몸을 돌리면
  그 회전이 갈 데가 없어 팔에 얹힌다 (활이 T자가 된 이유).
- **넓은 바닥에 한 장짜리 텍스처를 반복해 깔면 바둑판이 뜬다.** 반복수나
  노멀 세기로는 안 없어진다 — 확률적 타일링(`#breakTiling`)이 답이다.
- **`presentGear` 처럼 '연출이 끝나야 풀리는' 일시정지**는 그 연출의 시계가
  어디 붙어 있는지 반드시 본다. `draw()` 에 붙어 있으면 화면을 안 그리는
  동안 영영 안 끝난다 — 지금은 `Game.drive()` 로 빼 뒀다.
- 화면을 확인할 때 `__shot` 은 **HUD 를 SVG 로 합성하다가 컷신 요소의
  검은 배경이 화면을 덮는 일**이 있다. 3D 만 보려면 캔버스를
  `toDataURL` 해서 `/__drop` 으로 보내는 쪽이 확실하다:
  ```js
  const url = __game.render3d.canvas.toDataURL('image/jpeg', 0.86)
  await fetch('/__drop', {method:'POST', headers:{'content-type':'application/json'},
    body: JSON.stringify({name:'a.jpg', data:url})})
  ```
